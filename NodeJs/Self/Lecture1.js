/* Printing
function Hello (name){
    console.log('With + => Hello '+name);
    console.log('With , => Hello',name);}

// Hello('Rana');
var name = "Name";
Hello(name);
/**/
// setTimeout(seconds);    //to call a function after a delay of a few seconds
// clearTimeout(seconds);  //
// setInterval(value);
// clearInterval(value);   //it keeps calling